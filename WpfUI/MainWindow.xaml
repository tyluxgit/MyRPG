<Window x:Class="WpfUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Engine.ViewModels;assembly=Engine"
        mc:Ignorable="d"
        Title="Tyl's Awesome Game" Height="768" Width="1024">

    <Window.DataContext>
        <local:GameSession/>
    </Window.DataContext>

    <Window.Resources>
        <!-- Ressources globales pour les marges -->
        <Thickness x:Key="OuterMargin">4</Thickness>
        <Thickness x:Key="InnerMargin">2</Thickness>

        <!-- Convertisseur pour la visibilité -->
        <BooleanToVisibilityConverter x:Key="BooleanToVisibility" />

        <!-- Style partagé pour les boutons de navigation -->
        <Style x:Key="NavigationButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="25" />
            <Setter Property="Width" Value="65" />
            <Setter Property="Margin" Value="{StaticResource OuterMargin}" />
        </Style>
    </Window.Resources>

    <!-- Utilisation d'une marge globale pour le Grid principal -->
    <Grid Margin="{StaticResource OuterMargin}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="225" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Menu Label -->
        <Label Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
               Content="Menu" Background="AliceBlue" Padding="{StaticResource InnerMargin}" />

        <!-- Informations sur le joueur -->
        <Grid Grid.Row="1" Grid.Column="0" Background="LightBlue" Margin="{StaticResource OuterMargin}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Label Grid.Row="0" Grid.Column="0" Content="Name:"/>
            <Label Grid.Row="0" Grid.Column="1" Content="{Binding CurrentPlayer.Name}" />
            <Label Grid.Row="1" Grid.Column="0" Content="Class:"/>
            <Label Grid.Row="1" Grid.Column="1" Content="{Binding CurrentPlayer.CharacterClass}" />
            <Label Grid.Row="2" Grid.Column="0" Content="Life:"/>
            <Label Grid.Row="2" Grid.Column="1" Content="{Binding CurrentPlayer.HitPoints}" />
            <Label Grid.Row="3" Grid.Column="0" Content="Gold:"/>
            <Label Grid.Row="3" Grid.Column="1" Content="{Binding CurrentPlayer.Gold}" />
            <Label Grid.Row="4" Grid.Column="0" Content="XP:"/>
            <Label Grid.Row="4" Grid.Column="1" Content="{Binding CurrentPlayer.ExperiencePoints}" />
            <Label Grid.Row="5" Grid.Column="0" Content="Level:"/>
            <Label Grid.Row="5" Grid.Column="1" Content="{Binding CurrentPlayer.Level}" />
        </Grid>

        <!-- Zone Principale : Localisation et Monstre -->
        <Grid Grid.Row="1" Grid.Column="1" Background="LightGoldenrodYellow" Margin="{StaticResource OuterMargin}">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <!-- Localisation -->
            <Border Grid.Row="0" Grid.Column="1"
                    BorderBrush="LightGoldenrodYellow" BorderThickness="1" Margin="{StaticResource OuterMargin}">
                <Grid Margin="{StaticResource InnerMargin}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" HorizontalAlignment="Center"
                               Text="{Binding CurrentLocation.Name}" FontWeight="Bold" />
                    <Image Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center"
                           Source="{Binding CurrentLocation.ImageName}" Stretch="Uniform" />
                    <TextBlock Grid.Row="2" HorizontalAlignment="Center"
                               Text="{Binding CurrentLocation.Description}" TextWrapping="Wrap" Margin="{StaticResource InnerMargin}" />
                </Grid>
            </Border>

            <!-- Informations sur le Monstre -->
            <Border Grid.Row="1" Grid.Column="1"
                    BorderBrush="LightCoral" BorderThickness="5" Margin="{StaticResource OuterMargin}"
                    Visibility="{Binding HasMonster, Converter={StaticResource BooleanToVisibility}}">
                <Grid Margin="{StaticResource InnerMargin}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" HorizontalAlignment="Center"
                               Text="{Binding CurrentMonster.Name}" FontWeight="Bold" />
                    <Image Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center"
                           Source="{Binding CurrentMonster.ImageName}" Stretch="Uniform" />
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center"
                                Visibility="{Binding HasMonster, Converter={StaticResource BooleanToVisibility}}">
                        <TextBlock Text="Current Hit Points:" Margin="{StaticResource InnerMargin}" />
                        <TextBlock Text="{Binding CurrentMonster.HitPoints}" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- Bas de l'IU : Inventaire, Quêtes -->
        <Grid Grid.Row="2" Grid.Column="0" Background="BurlyWood" Margin="{StaticResource OuterMargin}">
            <TabControl Margin="{StaticResource OuterMargin}">
                <TabItem Header="Inventory">
                    <DataGrid ItemsSource="{Binding CurrentPlayer.Inventory}"
                              AutoGenerateColumns="False" HeadersVisibility="Column">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Description"
                                                Binding="{Binding Name}" Width="*" />
                            <DataGridTextColumn Header="Price"
                                                Binding="{Binding Price}" Width="Auto" />
                        </DataGrid.Columns>
                    </DataGrid>
                </TabItem>
                <TabItem Header="Quests">
                    <DataGrid ItemsSource="{Binding CurrentPlayer.Quests}"
                              AutoGenerateColumns="False" HeadersVisibility="Column">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name"
                                                Binding="{Binding PlayerQuest.Name}" Width="*" />
                            <DataGridTextColumn Header="Done?"
                                                Binding="{Binding IsCompleted}" Width="Auto" />
                        </DataGrid.Columns>
                    </DataGrid>
                </TabItem>
            </TabControl>
        </Grid>

        <!-- Panneau de Navigation -->
        <Grid Grid.Row="2" Grid.Column="1" Background="LightBlue" Margin="{StaticResource OuterMargin}">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="255" />
            </Grid.ColumnDefinitions>
            <Grid Grid.Row="0" Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <!-- Boutons de navigation avec Style appliqué -->
                <Button Grid.Row="0" Grid.Column="1" 
                        Style="{StaticResource NavigationButtonStyle}"
                        Click="OnClick_MoveNorth"
                        IsEnabled="{Binding HasLocationToNorth}"
                        Content="North"/>
                <Button Grid.Row="1" Grid.Column="0" 
                        Style="{StaticResource NavigationButtonStyle}"
                        Click="OnClick_MoveWest"
                        IsEnabled="{Binding HasLocationToWest}"
                        Content="West"/>
                <Button Grid.Row="1" Grid.Column="2" 
                        Style="{StaticResource NavigationButtonStyle}"
                        Click="OnClick_MoveEast"
                        IsEnabled="{Binding HasLocationToEast}"
                        Content="East"/>
                <Button Grid.Row="2" Grid.Column="1" 
                        Style="{StaticResource NavigationButtonStyle}"
                        Click="OnClick_MoveSouth"
                        IsEnabled="{Binding HasLocationToSouth}"
                        Content="South"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
